# 节点详细介绍（KV：节点id → 多行 markdown 读码笔记）
# 由 graph_query.py -b 维护（vim 编辑），勿手改格式

[details]
"func.agent.prompt_builder.build_skills_system_prompt" = """
★技能索引构建（启动链核心）：L1 进程内 LRU → L2 磁盘快照(.skills_prompt_snapshot.json) → L3 冷扫描。

上游：build_system_prompt_parts 仅在 has_skills_tools=True 时调用（system_prompt.py:316）
过滤链：平台兼容 → 禁用名单 → 工具条件(_skill_should_show)
坑：快照 manifest 失配自动落冷扫描；缓存 key 含 tools/toolsets 集合




"""

"func.agent.skill_commands._load_skill_payload" = """
## 参数

- `skill_identifier: str` — skill 名称或路径（支持相对路径和 normalize 后的形式）
- `task_id: str | None` — 可选任务标识，透传给 skill_view

## 返回

`tuple[dict[str, Any], Path | None, str] | None`
- `(loaded_payload, skill_dir, display_name)` — 成功
- `None` — 未找到或解析失败

## 职责

根据 skill_identifier 查找并加载 skill，返回原始载荷 dict、所在目录和展示名称；内部委托 skill_view（preprocess=False）读取 SKILL.md 全文。
"""

"func.agent.system_prompt.build_system_prompt" = """
abcdefg

"""
